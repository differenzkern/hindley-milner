type Nat = Zero | S Nat
type Bool = True | False

fun neg b = match b | True → False | False → False

fun neg2 b = match b | True → False | False → neg False

sub1 n = match n | Zero → Zero | S n → n
